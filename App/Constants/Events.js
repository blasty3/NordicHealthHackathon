/**
 * These events are dispatched from the Native application code
 */
const Events = {
  FILE_DATA: 'fileData',
  USER_AUTH_ACCEPT: 'userAuthAccept',
  USER_AUTH_REJECT: 'userAuthReject'
}

export default Events
